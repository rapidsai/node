{
  "name": "@rapidsai/cugraph",
  "version": "22.6.2",
  "license": "Apache-2.0",
  "author": "NVIDIA, Inc. (https://nvidia.com/)",
  "maintainers": [
    "Paul Taylor <paul.e.taylor@me.com>"
  ],
  "main": "index.js",
  "types": "build/js",
  "scripts": {
    "install": "npx rapidsai-install-native-module",
    "clean": "rimraf build doc compile_commands.json",
    "doc": "rimraf doc && typedoc --options typedoc.js",
    "test": "node -r dotenv/config node_modules/.bin/jest -c jest.config.js",
    "build": "yarn tsc:build && yarn cpp:build",
    "build:debug": "yarn tsc:build && yarn cpp:build:debug",
    "compile": "yarn tsc:build && yarn cpp:compile",
    "compile:debug": "yarn tsc:build && yarn cpp:compile:debug",
    "rebuild": "yarn tsc:build && yarn cpp:rebuild",
    "rebuild:debug": "yarn tsc:build && yarn cpp:rebuild:debug",
    "cpp:clean": "npx nvidia-cmake-js clean",
    "cpp:clean:debug": "npx nvidia-cmake-js clean -D",
    "cpp:build": "npx nvidia-cmake-js build -g",
    "cpp:build:debug": "npx nvidia-cmake-js build -g -D",
    "cpp:compile": "npx nvidia-cmake-js compile -g",
    "cpp:compile:debug": "npx nvidia-cmake-js compile -g -D",
    "cpp:configure": "npx nvidia-cmake-js configure -g",
    "cpp:configure:debug": "npx nvidia-cmake-js configure -g -D",
    "cpp:rebuild": "npx nvidia-cmake-js rebuild -g",
    "cpp:rebuild:debug": "npx nvidia-cmake-js rebuild -g -D",
    "cpp:reconfigure": "yarn cpp:clean && nvidia-cmake-js configure -g",
    "cpp:reconfigure:debug": "yarn cpp:clean && nvidia-cmake-js configure -g -D",
    "tsc:build": "rimraf build/js && tsc -p ./tsconfig.json",
    "tsc:watch": "rimraf build/js && tsc -p ./tsconfig.json -w",
    "dev:cpack": "cd build/Release && cpack -G TGZ >/dev/null && echo \"$(realpath -m ./rapidsai_*-*-*.tar.gz)\""
  },
  "dependencies": {
    "@rapidsai/cudf": "22.6.2"
  },
  "devDependencies": {
    "@rapidsai/cuml": "22.6.2"
  },
  "files": [
    "LICENSE",
    "README.md",
    "index.js",
    "package.json",
    "CMakeLists.txt",
    "src/node_cugraph",
    "build/js"
  ]
}
